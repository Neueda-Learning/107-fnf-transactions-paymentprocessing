spring:
  datasource:
    url: jdbc:mysql://localhost:3306/payments_db?createDatabaseIfNotExist=true
    username: root
    password: n3u3da!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true